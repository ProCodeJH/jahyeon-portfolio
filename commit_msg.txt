feat: Add Move Folder feature for nesting existing folders

NEW FEATURE - Move Folder:
- Blue folder move button (📂→) in folder action bar
- Move Folder dialog with parent folder selector
- Can move any existing folder into another folder
- Preview shows folder structure after move
- Prevents circular references (can't move parent into child)

UI:
- Button appears only for folders saved in DB (with id)
- Drop-down lists all folders in same category except self
- Root option to move folder back to top level
