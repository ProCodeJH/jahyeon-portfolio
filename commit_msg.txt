fix: Resources folder toggle - proper nested display

BEFORE (broken):
- All folders showed as separate cards
- Subfolders had ml-6 margin but appeared outside parent
- Folders open by default

AFTER (fixed):
- Only root folders shown initially (all closed)
- Click folder to expand -> shows files
- Subfolders appear INSIDE parent when expanded
- Click subfolder to expand -> shows its files
- Proper visual hierarchy with:
  - ↳ arrow prefix for subfolders
  - Blue/cyan gradient icon for subfolders
  - ml-4 indentation inside parent container
