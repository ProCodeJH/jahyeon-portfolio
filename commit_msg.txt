feat: Add Firebase Phone Auth for free SMS verification

- Install Firebase SDK
- Add firebase.ts config with Phone Auth helpers
- Update Register.tsx to use Firebase Phone Auth
  - reCAPTCHA verification
  - SMS sending via Firebase
  - Code verification via Firebase
- Add .env.example for Firebase config setup

Setup required:
1. Create Firebase project
2. Enable Phone Auth in Firebase Console
3. Add domain to authorized domains
4. Copy Firebase config to .env.local

Free tier: 10,000 SMS/month
