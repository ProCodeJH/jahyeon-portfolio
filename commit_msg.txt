fix: Add members API routes to Vercel serverless function

ROOT CAUSE OF 404 ERROR:
- api/index.ts (Vercel serverless) lacked members routes
- server/routers.ts has them but isn't used in production Vercel

ADDED:
- members table schema in api/index.ts
- members.register endpoint with access code verification
- members.login endpoint with password check

ACCESS CODE SYSTEM:
- Default code: "코딩쏙2024"
- Can be changed via settings key "student_access_code"
- Only users with correct code become students
